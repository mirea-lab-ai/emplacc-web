services:
  frontend:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-https://api.emplacc.g-309.ru}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - emplacc-network

  # MCP сервер — Remote MCP для Claude Code / OpenCode
  # URL: https://твой-домен/mcp?token=emplacc_...
  mcp:
    build:
      context: ../../emplacc-mcp
      dockerfile: Dockerfile
    environment:
      - EMPLACC_API_URL=${EMPLACC_API_URL:-http://api:8081}
      - PORT=3001
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - emplacc-network

  # Nginx reverse proxy (опционально)
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro  # для SSL сертификатов
    depends_on:
      - frontend
    restart: unless-stopped
    networks:
      - emplacc-network
    profiles:
      - proxy  # запускается только с профилем proxy

networks:
  emplacc-network:
    driver: bridge

volumes:
  ssl_certs:
    driver: local
